# Requirements

Restore Sinjoh Flap launches after the live Robinhood Chain Flap Portal upgraded from
`v5.15.2` to `v5.21.2`. Preserve fail-closed launch checks, verify that the supported
native-ETH Tax Token V3 launch configuration is unchanged, and prove that a newly launched
stock raffle can deploy, bind, receive launch revenue, draw, and pay a winner. Restore the
letscash.fun launch path after its in-place factory upgrade changed the launch tuple ABI.
